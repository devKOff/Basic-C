#include <stdio.h>

    int main() {
        int x;
        printf("Enter the value of x = ");
        scanf("%d", &x);
        printf("The value of x is %d\n", x);

        char operator;
        printf("Enter the operator : ");
        scanf(" %c", &operator);
        printf("The operator is %c",operator);

        return 0;
    }
